<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fernandopaiva_larissamoreira.appn1">

    <!-- Declaração da aplicação -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppN1"
        tools:targetApi="31">

        <!-- Atividade principal que é lançada quando o aplicativo é aberto -->
        <activity
            android:name=".core.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".core.SecondActivity" android:exported="false" />
        <activity android:name=".codificador.CodificadorActivity" android:exported="false" />
        <activity android:name=".pedra_papel_tesoura.PedraPapelTesouraActivity" android:exported="false" />
        <activity android:name=".codigo_secreto.CodigoSecretoActivity" android:exported="false" />
        <activity android:name=".codigo_secreto.CodigoSecretoResultadoActivity" android:exported="false" />
        <activity android:name=".randomizador.RandomizadorActivity" android:exported="false" />
    </application>
</manifest>
